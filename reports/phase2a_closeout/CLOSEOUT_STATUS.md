# Phase 2A 收口状态

基线提交：`b67033c4ba1ac994d25ee6d52c525e8e8ff58e5d`

分支：`feature/phase2a-data-geometry`

收口日期：2026-08-29

## 当前状态

| 项目 | 状态 | 边界 |
|---|---|---|
| 几何一致性接口、合成测试、H3 接线 | 已完成 | 只证明计算与梯度链路可用，不代表指标提升 |
| 下载和归档完整性检查 | 已执行 | 已保留实际大小、MD5 和工具错误证据 |
| 正式无标签池接入 | `BLOCKED_ACCESS` + `BLOCKED_INTEGRITY` | 未取得适用于本项目的明确授权；公开包容器检查失败 |
| 最终验收无标签候选 | 0 | 30,407 只是流式异常前观察下界，不是可训练数量 |
| 正式半监督训练 | 未开始 | 没有 EMA、伪标签、置信度筛选或新增训练成绩 |

H3 的结构、配置、best/last 和历史报告保持冻结。本轮 GPU 训练预算为 0，没有重新训练、重复下载、强制修复归档或改动模型。

## 进入正式训练前仍需统一

- `tx=0.1` 是归一化网格平移；在 512、`align_corners=True` 下为 25.55 像素，不是图宽 10%。
- 角度项单位为度，坐标项为归一化距离；当前 `0.1` 是接口验收系数，不是已确定的最优训练权重。
- H3 现有监督仍以 B2（MSE + 10×坐标 SmoothL1 + JS）作为工程参照，是否作为正式监督口径需导师看过证据后决定。
- 无标签真实可见性不能用接线检查里的全 True mask 代替。
- 退化结构或整个 batch 无有效几何时必须记录，不能把零辅助项当作优化成功。
- 当前只证明梯度能回传，不证明 MRE 或 AoP 改善。

## 正确推进顺序

1. 先把 [给导师的进展稿](ADVISOR_PROGRESS_DRAFT.md) 发给导师，询问课题组是否已有允许本毕业设计使用的完整数据副本或已获批访问路径。
2. 若课题组已有：取得明确使用范围、版本身份、原始归档和官方校验信息，再走[接收验收清单](APPROVED_ARCHIVE_ACCEPTANCE_CHECKLIST.md)。
3. 若课题组没有：由用户填写身份占位符后，使用[组织者咨询模板](ORGANIZER_ACCESS_EMAIL_TEMPLATE.md)询问当前研究申请流程；不直接签署互相冲突或可能过时的协议。
4. 只有授权证据和完整归档都通过验收，才进入下一轮真实半监督实验。

邮件均为未发送草稿。仓库没有保存真实身份、签名、密码、令牌、医学图像、逐样本文件名或导师原始方案。
